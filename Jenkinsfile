@Library('todo') _
todoshop (
        COMPONENT             : 'login',
        PROJECT_NAME          : "todo",
        SLAVE_LABEL           : "GOLANG",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "GOLANG"
)
